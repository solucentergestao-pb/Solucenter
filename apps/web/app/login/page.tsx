"use client";import {useState} from 'react';import {api} from '../../lib/api';export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');async function submit(e:any){e.preventDefault();try{const r=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('token',r.accessToken);location.href='/'}catch(e:any){setError(e.message)}}return <main className="shell"><div className="card" style={{maxWidth:420,margin:'60px auto'}}><h1>Entrar</h1><form onSubmit={submit}><div className="field">E-mail<input className="input" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field">Senha<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<p>{error}</p>}<button className="btn">Entrar</button></form></div></main>}
