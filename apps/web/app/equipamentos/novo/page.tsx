"use client";import {useEffect,useState} from 'react';import {api} from '../../../lib/api';
export default function NovoEquip(){const[customers,setCustomers]=useState<any[]>([]);const[units,setUnits]=useState<any[]>([]);const[envs,setEnvs]=useState<any[]>([]);const[created,setCreated]=useState<any>();const[f,setF]=useState<any>({customerId:'',unitId:'',environmentId:'',equipmentType:'SPLIT',capacityBtu:12000,technology:'INVERTER',voltage:220,refrigerant:'R32',serialNumber:'',installation:{liquidPipe:'',gasPipe:'',pipeLengthM:0,vacuumMicrons:0,tightnessTest:false}});useEffect(()=>{api('/customers').then(setCustomers)},[]);function customer(id:string){setF({...f,customerId:id,unitId:'',environmentId:''});setUnits(customers.find(x=>x.id===id)?.units??[]);setEnvs([])}async function unit(id:string){setF({...f,unitId:id,environmentId:''});setEnvs(await api('/units/'+id+'/environments'))}async function save(e:any){e.preventDefault();const body={...f,environmentId:f.environmentId||undefined};setCreated(await api('/equipment',{method:'POST',body:JSON.stringify(body)}))}async function qr(){const b=await api('/equipment/'+created.id+'/qr');const url=URL.createObjectURL(b as Blob);window.open(url,'_blank')}async function photo(file:File){const fd=new FormData();fd.append('category','NAMEPLATE');fd.append('file',file);await api('/uploads/equipment/'+created.id+'/photo',{method:'POST',body:fd});alert('Foto enviada')}return <main><h1>Novo equipamento</h1><form className="card grid" onSubmit={save}><select required value={f.customerId} onChange={e=>customer(e.target.value)}><option value="">Cliente</option>{customers.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select><select required value={f.unitId} onChange={e=>unit(e.target.value)}><option value="">Unidade</option>{units.map(u=><option value={u.id} key={u.id}>{u.name}</option>)}</select><select value={f.environmentId} onChange={e=>setF({...f,environmentId:e.target.value})}><option value="">Ambiente</option>{envs.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select>{['equipmentType','serialNumber','technology','refrigerant'].map(k=><input key={k} placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<input type="number" placeholder="BTU/h" value={f.capacityBtu} onChange={e=>setF({...f,capacityBtu:+e.target.value})}/><input type="number" placeholder="Tensão" value={f.voltage} onChange={e=>setF({...f,voltage:+e.target.value})}/><h3>Instalação</h3><input placeholder="Bitola líquido" onChange={e=>setF({...f,installation:{...f.installation,liquidPipe:e.target.value}})}/><input placeholder="Bitola gás" onChange={e=>setF({...f,installation:{...f.installation,gasPipe:e.target.value}})}/><input type="number" placeholder="Comprimento linha (m)" onChange={e=>setF({...f,installation:{...f.installation,pipeLengthM:+e.target.value}})}/><input type="number" placeholder="Vácuo (microns)" onChange={e=>setF({...f,installation:{...f.installation,vacuumMicrons:+e.target.value}})}/><label><input type="checkbox" onChange={e=>setF({...f,installation:{...f.installation,tightnessTest:e.target.checked}})}/> Teste de estanqueidade aprovado</label><button>Salvar e gerar QR Code</button></form>{created&&<section className="card"><h2>{created.assetCode}</h2><p>Equipamento cadastrado.</p><button type="button" onClick={qr}>Abrir QR Code</button><p><input type="file" accept="image/*" onChange={e=>e.target.files?.[0]&&photo(e.target.files[0])}/></p></section>}</main>}