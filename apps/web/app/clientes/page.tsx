"use client";import {useEffect,useState} from 'react';import {api} from '../../lib/api';export default function Clientes(){const[list,setList]=useState<any[]>([]),[name,setName]=useState(''),[type,setType]=useState('PJ'),[whatsapp,setWhatsapp]=useState('');async function load(){setList(await api('/customers'))}useEffect(()=>{load().catch(()=>{})},[]);async function add(e:any){e.preventDefault();await api('/customers',{method:'POST',body:JSON.stringify({name,type,whatsapp})});setName('');await load()}return <main className="shell"><h1>Clientes</h1><div className="grid"><form className="card" onSubmit={add}><h3>Novo cliente</h3><div className="field">Tipo<select value={type} onChange={e=>setType(e.target.value)}><option>PJ</option><option>PF</option></select></div><div className="field">Nome / Razão Social<input className="input" value={name} onChange={e=>setName(e.target.value)} required/></div><div className="field">WhatsApp<input className="input" value={whatsapp} onChange={e=>setWhatsapp(e.target.value)}/></div><button className="btn">Salvar cliente</button></form><div className="card"><h3>Clientes cadastrados</h3>{list.map(c=><div key={c.id} style={{padding:'10px 0',borderBottom:'1px solid #eee'}}><b>{c.name}</b><br/><small>{c.code} · {c.units.length} unidade(s)</small></div>)}</div></div></main>}
