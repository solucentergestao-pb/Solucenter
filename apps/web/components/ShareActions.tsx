'use client';
export function ShareActions({url,title='Documento Solucenter',message='Olá! Segue o documento da Solucenter:'}:{url:string,title?:string,message?:string}){const text=`${message} ${url}`;const share=async()=>{if(navigator.share)await navigator.share({title,text:message,url});else await navigator.clipboard.writeText(url)};return <div className="shareActions"><a className="btn primary" target="_blank" rel="noreferrer" href={`https://wa.me/?text=${encodeURIComponent(text)}`}>WhatsApp</a><a className="btn" href={`mailto:?subject=${encodeURIComponent(title)}&body=${encodeURIComponent(text)}`}>E-mail</a><button className="btn" onClick={share}>Compartilhar</button></div>}
