'use client';
import {useState} from 'react';import {AppShell} from '../../components/AppShell';import {api} from '../../lib/api';import {ShareActions} from '../../components/ShareActions';
export default function Documents(){const [kind,setKind]=useState<'quote'|'os'>('quote'),[id,setId]=useState(''),[doc,setDoc]=useState<any>(null),[err,setErr]=useState('');async function generate(){setErr('');setDoc(null);try{const path=kind==='quote'?`/documents/quotes/${id}/pdf`:`/documents/service-orders/${id}/pdf`;setDoc(await api(path,{method:'POST'}))}catch(e:any){setErr(e.message)}}return <AppShell><section className="hero"><div><span className="eyebrow">DOCUMENTOS</span><h1>Gerar e compartilhar</h1><p>Orçamentos e relatórios técnicos com identidade Solucenter.</p></div></section><div className="card form"><label>Tipo de documento<select value={kind} onChange={e=>setKind(e.target.value as any)}><option value="quote">Orçamento comercial</option><option value="os">OS / relatório técnico</option></select></label><label>ID interno<input value={id} onChange={e=>setId(e.target.value)} placeholder="UUID do orçamento ou OS"/></label><button className="btn primary" onClick={generate} disabled={!id}>Gerar PDF</button>{err&&<p className="error">{err}</p>}{doc?.shareUrl&&<div className="card"><h3>Documento pronto</h3><p className="muted">Use o link seguro para enviar ao cliente.</p><ShareActions url={doc.shareUrl} title={kind==='quote'?'Orçamento Solucenter':'Relatório técnico Solucenter'}/></div>}</div></AppShell>}
