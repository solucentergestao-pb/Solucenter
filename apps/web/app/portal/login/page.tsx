'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
const BASE=process.env.NEXT_PUBLIC_API_URL??'http://localhost:3333/api/v1';
export default function PortalLogin(){const r=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function submit(e:any){e.preventDefault();setError('');const res=await fetch(BASE+'/portal/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const b=await res.json();if(!res.ok)return setError(b?.error?.message??'Não foi possível entrar.');localStorage.setItem('portal_token',b.accessToken);r.push('/portal')}return <main className="wrap"><section className="card" style={{maxWidth:460,margin:'50px auto'}}><div className="eyebrow">ÁREA DO CLIENTE</div><h1>Solucenter</h1><p>Acompanhe seus equipamentos, serviços, orçamentos e manutenções.</p><form onSubmit={submit} className="form-grid"><label>E-mail<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Senha<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required minLength={6}/></label>{error&&<div className="alert">{error}</div>}<button className="primary">Entrar no portal</button></form></section></main>}
