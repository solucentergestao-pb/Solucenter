import { FastifyInstance } from 'fastify'; import { prisma } from '../lib/prisma.js'; import { AuthUser } from '../lib/auth.js'; export async function materialRoutes(app:FastifyInstance){app.addHook('preHandler',async r=>r.jwtVerify());app.get('/',async req=>{const u=req.user as AuthUser;return prisma.material.findMany({where:{companyId:u.companyId,active:true},orderBy:{description:'asc'}})})}
