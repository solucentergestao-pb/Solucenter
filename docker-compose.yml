services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: solucenter
      POSTGRES_PASSWORD: solucenter_dev
      POSTGRES_DB: solucenter
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
