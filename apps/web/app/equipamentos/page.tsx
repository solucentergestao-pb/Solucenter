"use client";import {useEffect,useState} from 'react';import {api} from '../../lib/api';export default function Equipamentos(){const[list,setList]=useState<any[]>([]);useEffect(()=>{api('/equipment').then(setList).catch(()=>{})},[]);return <main className="shell"><h1>Equipamentos</h1><div className="grid">{list.map(e=><div className="card" key={e.id}><b>{e.assetCode}</b><h3>{e.equipmentType} · {e.capacityBtu??'-'} BTU/h</h3><p>{e.customer.name}<br/>{e.unit.name}{e.environment?' · '+e.environment.name:''}</p></div>)}</div></main>}
