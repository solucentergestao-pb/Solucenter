import {FastifyInstance} from 'fastify'; import {prisma} from '../lib/prisma.js'; import {AuthUser} from '../lib/auth.js'; import {fail} from '../lib/errors.js'; import {createBusinessPdf} from '../lib/pdf.js';
const money=(n:any)=>Number(n??0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
export async function documentRoutes(app:FastifyInstance){
 app.post('/quotes/:id/pdf',{preHandler:async r=>r.jwtVerify()},async(req,reply)=>{const u=req.user as AuthUser,{id}=req.params as any;const q=await prisma.quote.findFirst({where:{id,companyId:u.companyId},include:{customer:true,items:true,company:true}});if(!q)return fail(reply,404,'QUOTE_NOT_FOUND','Orçamento não encontrado.');const pdf=await createBusinessPdf({title:'ORÇAMENTO COMERCIAL',number:q.quoteNumber,company:q.company.tradeName,logoUrl:q.company.logoUrl,customer:q.customer.name,lines:[{label:'Subtotal',value:money(q.subtotal)},{label:'Desconto',value:money(q.discount)},{label:'Total',value:money(q.total)},{label:'Validade',value:q.validUntil?.toLocaleDateString('pt-BR')??'-'}],sections:[{title:'Serviços e materiais',body:q.items.map(i=>`${i.description} — ${i.quantity} × ${money(i.unitPrice)} = ${money(Number(i.quantity)*Number(i.unitPrice))}`).join('\n')},{title:'Condições de pagamento',body:q.paymentTerms??'-'},{title:'Garantia',body:q.warrantyTerms??'-'},{title:'Observações',body:q.notes??'-'}],footer:`${q.company.tradeName} • Documento comercial Solucenter`});const d=await prisma.generatedDocument.create({data:{companyId:u.companyId,type:'QUOTE',entityId:q.id,fileUrl:pdf.fileUrl,publicToken:pdf.token}});return {document:d,shareUrl:`${process.env.PUBLIC_API_URL??'http://localhost:3333'}/public/documents/${pdf.token}`}});
 app.post('/service-orders/:id/pdf',{preHandler:async r=>r.jwtVerify()},async(req,reply)=>{const u=req.user as AuthUser,{id}=req.params as any;const o=await prisma.serviceOrder.findFirst({where:{id,companyId:u.companyId},include:{customer:true,equipment:true,materials:{include:{material:true}},company:true,photos:true,signatures:{orderBy:{signedAt:'desc'},take:1},measurements:true}});if(!o)return fail(reply,404,'ORDER_NOT_FOUND','OS não encontrada.');const last=o.measurements.at(-1);const pdf=await createBusinessPdf({title:'ORDEM DE SERVIÇO / RELATÓRIO TÉCNICO',number:o.orderNumber,company:o.company.tradeName,logoUrl:o.company.logoUrl,customer:o.customer.name,lines:[{label:'Equipamento',value:o.equipment?.assetCode??'-'},{label:'Status',value:o.status},{label:'Valor do serviço',value:money(o.finalValue)},{label:'Medição ΔT',value:last?.deltaT?`${last.deltaT} °C`:'-'}],sections:[{title:'Problema relatado',body:o.reportedProblem},{title:'Diagnóstico técnico',body:o.technicalDiagnosis??'-'},{title:'Serviço executado',body:o.performedService??'-'},{title:'Materiais utilizados',body:o.materials.map(m=>`${m.material.description}: ${m.quantity} ${m.material.unit}`).join('\n')||'-'},{title:'Recomendações',body:o.recommendations??'-'}],photos:o.photos.map(p=>({fileUrl:p.fileUrl,caption:p.description??p.category})),signature:o.signatures[0]?{name:o.signatures[0].signerName,fileUrl:o.signatures[0].fileUrl}:null,footer:`${o.company.tradeName} • Relatório técnico Solucenter`});const d=await prisma.generatedDocument.create({data:{companyId:u.companyId,type:'TECHNICAL_REPORT',entityId:o.id,fileUrl:pdf.fileUrl,publicToken:pdf.token}});return {document:d,shareUrl:`${process.env.PUBLIC_API_URL??'http://localhost:3333'}/public/documents/${pdf.token}`}});
 app.get('/public/:token',async(req,reply)=>{const {token}=req.params as any;const d=await prisma.generatedDocument.findUnique({where:{publicToken:token}});if(!d||d.expiresAt&&d.expiresAt<new Date())return fail(reply,404,'DOCUMENT_NOT_FOUND','Documento indisponível.');return {type:d.type,fileUrl:d.fileUrl,createdAt:d.createdAt};});
}
export async function publicDocumentRoutes(app:FastifyInstance){app.get('/:token',async(req,reply)=>{const {token}=req.params as any;const d=await prisma.generatedDocument.findUnique({where:{publicToken:token}});if(!d||d.expiresAt&&d.expiresAt<new Date())return reply.code(404).type('text/html').send('<h1>Documento indisponível</h1>');return reply.redirect(d.fileUrl);});}
