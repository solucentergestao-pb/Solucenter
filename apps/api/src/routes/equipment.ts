import { FastifyInstance } from 'fastify'; import { z } from 'zod'; import crypto from 'node:crypto'; import QRCode from 'qrcode'; import { prisma } from '../lib/prisma.js'; import { AuthUser } from '../lib/auth.js'; import { fail } from '../lib/errors.js'; import { audit } from '../lib/audit.js';
const schema=z.object({customerId:z.string().uuid(),unitId:z.string().uuid(),environmentId:z.string().uuid().optional().nullable(),modelId:z.string().uuid().optional().nullable(),equipmentType:z.string().min(2),capacityBtu:z.number().int().positive().optional(),technology:z.string().optional(),cycle:z.string().optional(),voltage:z.number().int().positive().optional(),phase:z.string().optional(),refrigerant:z.string().optional(),serialNumber:z.string().optional(),installationDate:z.coerce.date().optional(),warrantyUntil:z.coerce.date().optional(),notes:z.string().optional(),installation:z.object({liquidPipe:z.string().optional(),gasPipe:z.string().optional(),pipeLengthM:z.number().nonnegative().optional(),heightDifferenceM:z.number().nonnegative().optional(),powerCable:z.string().optional(),breakerRating:z.string().optional(),drainType:z.string().optional(),vacuumMicrons:z.number().int().nonnegative().optional(),tightnessTest:z.boolean().optional(),additionalRefrigerantG:z.number().int().nonnegative().optional()}).optional()});
export async function equipmentRoutes(app:FastifyInstance){app.addHook('preHandler',async r=>r.jwtVerify());
 app.get('/',async req=>{const u=req.user as AuthUser;return prisma.equipment.findMany({where:{companyId:u.companyId},include:{customer:true,unit:true,environment:true,model:{include:{manufacturer:true}},photos:true,qr:true},orderBy:{createdAt:'desc'}})});
 app.post('/',async(req,reply)=>{const u=req.user as AuthUser,p=schema.safeParse(req.body);if(!p.success)return fail(reply,422,'VALIDATION_ERROR','Equipamento inválido.',p.error.flatten());const unit=await prisma.customerUnit.findFirst({where:{id:p.data.unitId,customerId:p.data.customerId,customer:{companyId:u.companyId}}});if(!unit)return fail(reply,422,'INVALID_RELATION','Unidade não pertence ao cliente.');if(p.data.environmentId){const env=await prisma.environment.findFirst({where:{id:p.data.environmentId,unitId:p.data.unitId}});if(!env)return fail(reply,422,'INVALID_RELATION','Ambiente não pertence à unidade.')}const count=await prisma.equipment.count({where:{companyId:u.companyId}});const {installation,...data}=p.data;const equipment=await prisma.equipment.create({data:{...data,companyId:u.companyId,assetCode:`SC-EQP-${String(count+1).padStart(6,'0')}`,qr:{create:{token:crypto.randomBytes(24).toString('hex')}},...(installation?{installation:{create:installation}}:{})},include:{qr:true,installation:true}});await audit(req,'CREATE','Equipment',equipment.id,{assetCode:equipment.assetCode});return reply.code(201).send(equipment)});
 app.get('/:id/qr',async(req,reply)=>{const u=req.user as AuthUser,{id}=req.params as any;const eq=await prisma.equipment.findFirst({where:{id,companyId:u.companyId},include:{qr:true}});if(!eq?.qr)return fail(reply,404,'QR_NOT_FOUND','QR Code não encontrado.');const base=process.env.PUBLIC_APP_URL??'http://localhost:3000';const url=`${base}/q/${eq.qr.token}`;const png=await QRCode.toBuffer(url,{width:640,margin:2});reply.type('image/png').header('Content-Disposition',`inline; filename="${eq.assetCode}.png"`).send(png)});
}